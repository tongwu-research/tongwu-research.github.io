# Tong Wu — Academic homepage

A lightweight, responsive academic homepage for Tong Wu, built as a single static HTML file for GitHub Pages.

## Local preview

```bash
python3 -m http.server 8000
```

Then open `http://localhost:8000`.
